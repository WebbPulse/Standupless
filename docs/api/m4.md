# M4 API contract: planning

Fixed shape both halves build against, in the M1 to M3 style: routes under `/api`, list bodies are objects with one plural key beside `next_cursor`, timestamps ISO 8601 UTC, ids ULIDs, platform error envelope, and a caller outside a workspace sees 404 never 403. One domain lands here, `planning`, plus the one stream consumer it owns.

Everything is workspace scoped and reached through the M1 authorization dependency at `app/common/api/dependencies/authz.py`. No route here introduces a second way to decide access: a cycle and a milestone are readable exactly when their project is readable, and writable exactly when the caller is a project member. There is no new capability and no new role.

Route prefixes for the gateway, matching `lambda_domain_path_prefixes` in `terraform/apigateway.tf`:

| Domain | Prefix |
|---|---|
| `planning` | `/api/workspaces/{workspace_id}/cycles`, `/api/workspaces/{workspace_id}/milestones`, `/api/workspaces/{workspace_id}/roadmap` |

All three are ordinary siblings of the `issues` and `views` prefixes, so nothing here depends on route specificity the way M3's comment prefix does.

## Routes (`planning` domain)

### Cycles

| Method and path | Body in | Body out | Who |
|---|---|---|---|
| `GET /workspaces/{workspace_id}/cycles` | query `project_id` (required), `status?` (`upcoming`, `active`, `completed`, `cancelled`), `cursor?`, `limit?` (1 to 100, default 50) | `{"cycles": [Cycle], "next_cursor"}` ordered by `start_date` ascending | project reader |
| `POST /workspaces/{workspace_id}/cycles` | `{"project_id", "name", "start_date", "end_date", "goal"?}` | `Cycle`, 201 | project member |
| `GET /workspaces/{workspace_id}/cycles/{cycle_id}` | query `project_id` (required) | `Cycle` | project reader |
| `PATCH /workspaces/{workspace_id}/cycles/{cycle_id}` | `{"project_id"}` plus any of `name`, `start_date`, `end_date`, `goal`, `cancelled` | `Cycle` | project member |
| `DELETE /workspaces/{workspace_id}/cycles/{cycle_id}` | query `project_id` (required) | 204; issues pointing at it keep their `cycle_id` until they are next patched, and the row simply stops resolving | project admin |

`project_id` is required on every single-cycle route because the `planning` table's sort key is `project#<pid>#cycle#<cid>`: without the project the read would be a scan, which design section 2 forbids outside an admin path. It is a query parameter rather than a path segment so a cycle id stays stable in a permalink, exactly as M3 does for a comment's `issue_id`.

Overlapping cycles are allowed. The MVP states no "at most one active cycle" constraint, because enforcing it would need a range read and a conditional write on every create, and a project running two overlapping time boxes is a planning mistake rather than a data one.

### Milestones

| Method and path | Body in | Body out | Who |
|---|---|---|---|
| `GET /workspaces/{workspace_id}/milestones` | query `project_id` (required), `status?` (`planned`, `in_progress`, `done`), `cursor?`, `limit?` (1 to 100, default 50) | `{"milestones": [Milestone], "next_cursor"}` ordered by `target_date` ascending, undated last | project reader |
| `POST /workspaces/{workspace_id}/milestones` | `{"project_id", "name", "description"?, "target_date"?, "status"?}` | `Milestone`, 201; `status` defaults to `planned` | project member |
| `GET /workspaces/{workspace_id}/milestones/{milestone_id}` | query `project_id` (required) | `Milestone` | project reader |
| `PATCH /workspaces/{workspace_id}/milestones/{milestone_id}` | `{"project_id"}` plus any of `name`, `description`, `target_date`, `status` | `Milestone` | project member |
| `DELETE /workspaces/{workspace_id}/milestones/{milestone_id}` | query `project_id` (required) | 204 | project admin |

A milestone's `status` is explicit rather than derived, because unlike a cycle it has no start date and a target date alone cannot say whether work began. A cycle's status is derived and a milestone's is set, and the two shapes say so in their own fields.

### Roadmap

| Method and path | Body in | Body out | Who |
|---|---|---|---|
| `GET /workspaces/{workspace_id}/roadmap` | query `project_id?`, `kind?` (`cycle`, `milestone`), `cursor?`, `limit?` (1 to 100, default 50) | `{"entries": [RoadmapEntry], "next_cursor"}` ordered by `target_date` ascending, undated last | member; a guest sees only their projects |

The roadmap is one read across the projects the caller can see. Without `project_id` it fans out over exactly those projects, which for a guest is `AuthzContext.project_ids` and for everyone else is the workspace's projects, so there is no cross-project widening and no cross-workspace read by construction: `ws_project` begins with the workspace id.

Each project's slice is one query on `ws_project-target_date-index`, whose range key is a cycle's `end_date` or a milestone's `target_date`. A milestone with no target date carries no index range value and so sits outside the index; it is read from the project's own partition and appended after the dated entries, which is what "undated last" means rather than a second sort.

The cursor is a position in the merged order rather than a `LastEvaluatedKey`, for the reason M2 already fixed for the issue fan-out: a merged page has no single last evaluated key to hand back. With exactly one project named the read is still merged the same way, so one code path serves both and a cursor means the same thing either way.

### Issues

The `issues` domain gains two optional attributes rather than the `planning` domain gaining a write path onto an issue.

| Method and path | Change |
|---|---|
| `POST /workspaces/{workspace_id}/issues` | the create body accepts `cycle_id` and `milestone_id`, both optional and nullable |
| `PATCH /workspaces/{workspace_id}/issues/{issue_id}` | the patch body accepts `cycle_id` and `milestone_id`, both clearable with null |
| `GET /workspaces/{workspace_id}/issues` | the list query accepts `cycle_id` and `milestone_id` as filters, alongside the M2 set |

Both are validated against the same project the issue is in: a `cycle_id` or `milestone_id` naming a row in another project is 422, not a silently dangling pointer. The validation is a point read on `planning` under the issue's own project prefix, so it cannot resolve a row the issue's project does not own.

`Issue` gains `cycle_id` and `milestone_id`, both nullable, and each becomes the range value of the `ws_project-cycle_id-index` and `ws_project-milestone_id-index` the M2 table already declares and leaves sparse. An issue with neither set stays out of both indexes, which is what keeps an unplanned backlog free in index cost.

The `issues` domain reads `planning` and never writes it. The `planning` domain reads `issues` for nothing at all: it never queries them, and its counts arrive on the stream. That is the "rollups without a second write path into issues" design section 7 names.

### Consumers

| Method and path | Body in | Body out | Who |
|---|---|---|---|
| `POST /events` on `planning-rollup-consumer` | stream records | consumer only; a gateway request 404s | none |

It mounts through `webbpulse.events.register_stream_consumer`, which already guards the gateway case and returns the `ReportBatchItemFailures` envelope, exactly as the M2 rollup and the two M3 views consumers do. It runs the `planning` image under the entrypoint `app.domains.planning.consumers.rollup_entrypoint`.

## Shapes

`Cycle`: `cycle_id` (ULID), `workspace_id`, `project_id`, `name` (1 to 80 chars), `start_date` and `end_date` (`YYYY-MM-DD`, both required, `end_date` never before `start_date`), `goal` (nullable, at most 2000 chars), `cancelled` (boolean, default false), `status` (derived, see below), `counts` (`RollupCounts`), `created_by`, `created_at`, `updated_at`.

`Cycle.status` is derived on read from the dates and the flag, never stored: `cancelled` when the flag is set, otherwise `upcoming` while today is before `start_date`, `active` while today is between the two inclusive, and `completed` once today is past `end_date`. Deriving rather than storing is what keeps a cycle from needing a scheduled write to change state at midnight, and it means the status a reader sees is always the one the dates imply.

`Milestone`: `milestone_id` (ULID), `workspace_id`, `project_id`, `name` (1 to 80 chars), `description` (nullable markdown, at most 8192 bytes), `target_date` (nullable `YYYY-MM-DD`), `status` (`"planned" | "in_progress" | "done"`, default `planned`, stored), `counts` (`RollupCounts`), `created_by`, `created_at`, `updated_at`.

`RollupCounts`: `todo`, `in_progress`, `done`, `cancelled`, `total`, each a non-negative integer. The four buckets fold M1's five status categories, which are the project's own and so cannot be counted directly by a consumer that will not read a project's config per record: `backlog` and `unstarted` are `todo`, `started` is `in_progress`, `completed` is `done`, `cancelled` is `cancelled`. `total` is the sum, held as its own attribute so a reader never has to trust that four counters agree.

`RoadmapEntry`: `kind` (`"cycle" | "milestone"`), `id` (the cycle or milestone id), `project_id`, `name`, `target_date` (nullable; a cycle's `end_date` or a milestone's `target_date`), `start_date` (nullable, a cycle's only), `status` (the cycle's derived status or the milestone's stored one), `counts` (`RollupCounts`). A roadmap entry is deliberately a projection rather than the full row: the timeline renders a bar and a count, and a reader wanting the rest has the entity's own route.

## Consumer event contract

### `planning-rollup-consumer`

Source: the `issues` table stream, `NEW_AND_OLD_IMAGES`.

Filter pattern: `{"eventName": ["INSERT", "MODIFY", "REMOVE"]}`. A REMOVE has to be handled, otherwise a deleted issue stays counted against its cycle for good.

Both images are read with `webbpulse.events.deserialize_image(record, "NewImage")` and `"OldImage"`.

What it writes: for each record it computes the `(planning_key, bucket)` pair the old image contributed and the pair the new image contributes, for the cycle and the milestone independently. When the two differ it decrements the old counter and increments the new one with an atomic `ADD` on the planning row, so two records for the same cycle arriving on different shards do not lose a count to a read-modify-write.

The bucket comes from the issue's status category, which the consumer resolves by reading the project's statuses from `project_config` once per record and caching them for the invocation. That is a read of a table the domain already holds read access to, and it is the only way a status id becomes a category without the issue carrying one.

Counters are clamped at zero on read rather than on write: an `ADD -1` that would take a counter below zero is still applied, because refusing it conditionally would make a redelivery diverge, and the read path reports `max(0, value)`. A counter that drifts is repaired by the next full recount, which the MVP does not schedule and does not need: the drift window is a redelivered decrement, which the idempotency below already prevents.

Idempotency: an `ADD` is not idempotent by itself, so each record's effect is claimed first. The consumer writes `planning#<workspace_id>#<issue_id>#<sequence_number>` into `idempotency` conditional on absence, with a 24 hour TTL, and a record that loses the claim is a no-op. The sequence number is the stream record's own, which is stable across a redelivery of the same record and different for a genuinely later change, so a partial-batch retry re-reads the claim rather than double counting.

Nothing is ever written back into the `issues` table. The `planning` bundle holds `issues` as a read table and does not hold it as a write table, which is the invariant design section 7 names and the one a reviewer should check on the Terraform diff.

Failures raise per record, so only that record enters `batchItemFailures`.

## Tables this milestone adds

Same notation as M3, per `docs/design.md` section 3.

| Table | PK | SK | GSIs | TTL / stream |
|---|---|---|---|---|
| `planning` | `workspace_id` | `planning_key` (`project#<pid>#cycle#<cid>` or `project#<pid>#milestone#<mid>`) | `ws_project-target_date-index` (hash `ws_project` = `<workspace_id>#<project_id>`, range `target_date`) | none |

The table carries no stream of its own: nothing downstream reads a cycle's changes, and the one async effect in this milestone flows the other way, off the `issues` stream.

`target_date` is a denormalised attribute rather than the entity's own field: a cycle writes its `end_date` into it and a milestone writes its `target_date`, so one index orders both kinds on the one date a roadmap draws them at. A milestone with no target date writes no `target_date` attribute at all, which leaves it out of the index rather than sorting it under an empty string.

The `issues` table gains no change. Its two indexes `ws_project-cycle_id-index` and `ws_project-milestone_id-index`, declared in M2 and left sparse, become populated by this milestone's issue attributes. Its `NEW_AND_OLD_IMAGES` stream now feeds four mappings: the M2 rollup on the `issues` function, the two M3 `views` consumers, and this one. Four readers on one stream is at the point where design section 3's note applies, so a fifth wants EventBridge Pipes rather than a fifth direct reader. That is a post-MVP note, not an M4 change.

Repository bundles:

- `planning` writes `planning`, `idempotency`, `rate-limits`; reads `memberships`, `workspaces`, `users`, `projects`, `project_config`, `issues`.
- `issues` gains `planning` as a read table, so a create or patch can validate a `cycle_id` against the issue's own project.

`planning` reads `issues` and never writes it. That is what keeps the rollup off a second write path into the issue.

## Authorization rules

Every route here depends on the M1 `require(...)` dependency and declares one of the capabilities that already exist. No new `Capability` member is added.

- Cycle and milestone reads declare `Capability.WORKSPACE_READ` and then resolve the project from the required `project_id` parameter, checking `AuthzContext.can_see_project(project_id)`, which is a 404 when it is false. That is the same fail-closed shape M2 uses for an issue read and the same indistinguishable 404.
- Cycle and milestone writes need the caller to be a project member: workspace `owner`, `admin` or `member`, or a guest holding a membership in that project. A guest outside the project gets 404, never 403.
- Deleting a cycle or a milestone is a project admin, which is `require_project_admin` resolved against the row's own project. Creating and editing is any project member, because a cycle is planning rather than configuration and the whole team moves it.
- The roadmap enumerates visible projects from `AuthzContext`: `project_ids` for a guest, the workspace's projects otherwise. A `project_id` a guest cannot see is 404 on the roadmap, not an empty roadmap.
- Setting `cycle_id` or `milestone_id` on an issue needs nothing beyond the project membership the issue patch already needs, and the target must be in the issue's own project, so there is no route by which a cycle in one project collects another project's issues.
- The consumer carries no user. It reads the issue images off the stream and writes only counters on the planning rows they name. It never calls `require`, and a gateway request to `/events` is 404 by `register_stream_consumer`'s own guard.

The route-contract fixture and the per-domain route counts are regenerated with this milestone, per the invariant in design section 2, so the diff on that fixture is the review artifact for whether a route here became public.

## Error codes

Beyond the platform envelope's own `NOT_FOUND`, `FORBIDDEN`, `NOT_AUTHENTICATED`, `CONFLICT` and `VALIDATION_ERROR`:

| Code | Status | When |
|---|---|---|
| `NOT_FOUND` | 404 | The workspace, the project, the cycle or the milestone is absent, or the caller may not see the project it belongs to |
| `FORBIDDEN` | 403 | The caller is in the project but is not a member for a write, or not an admin for a delete |
| `VALIDATION_ERROR` | 422 | `end_date` is before `start_date`; a name is blank; a date is not `YYYY-MM-DD`; a `cycle_id` or `milestone_id` on an issue names a row outside that issue's project |
| `CONFLICT` | 409 | A cycle or milestone id collides, which is an id reuse rather than a lost race |

## Pagination

Unchanged from M2 and M3: an opaque base64url cursor minted and read by `app/common/api/pagination.py`, carrying the scope it was issued under so a cursor from one query is refused on another. Every list body is `{"<plural>": [...], "next_cursor": string or null}`, and `next_cursor` is null exactly when the set is exhausted.

Cycle and milestone lists page on a `LastEvaluatedKey` under the project's own sort key prefix, which is a real index position. The roadmap pages on a merged offset, for the reason above.

## Platform gaps

Verified against `webbpulse` 0.43.0 as installed. Most of design section 6 has landed. What M4 wanted and did not find:

| Gap | Where | Proposed surface |
|---|---|---|
| Atomic multi-counter update | `webbpulse-python` | `Repository.increment` moves one attribute by one delta and answers the new value. The rollup moves two counters on one row in one call, one down and one up, so the product writes the `ADD` expression by hand. `Repository.add(key, {"todo": -1, "in_progress": 1})` beside `increment` closes it, and every rollup consumer in the org wants the same shape. |
| List envelope with one plural key | `webbpulse-python` | Still open from M2 and M3. `http.CursorPage` renders items under `items`, and every list body in this product is an object with one plural key, so `cursor_page` is called with an item key the package builds a model around. Noted again because M4 adds three more list bodies through it. |
| Stream record sequence number | `webbpulse-python` | The consumer's idempotency key needs the record's own `dynamodb.SequenceNumber`, which it reads out of the raw mapping because `deserialize_image` returns only an image. `webbpulse.events.record_sequence(record)` beside it is three lines and every idempotent stream consumer needs it. |
| Derived status from a date range | not a platform gap | A cycle's `upcoming`, `active`, `completed` is product semantics and stays in the product. |
