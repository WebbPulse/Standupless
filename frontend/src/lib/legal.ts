/**
 * The facts the privacy policy and the terms both state: who runs the service,
 * how to reach them, and the date the current text took effect. Kept apart
 * from the page components so both pages and their tests read one copy.
 */

/** The address for privacy, account, legal and security questions. */
export const LEGAL_CONTACT_EMAIL = 'tyler@webbpulse.com';

/** The day the current privacy policy and terms took effect. */
export const LEGAL_EFFECTIVE_DATE = 'September 26, 2026';

/** The state whose law governs the terms. */
export const LEGAL_GOVERNING_STATE = 'California';
